# 🚀 CodeLab VS Code Plugin - Быстрый старт

## Предварительные требования

- Node.js 18+ и npm
- VS Code 1.109+
- CodeLab Backend API (запущен на `http://localhost:8000`)

## Установка и запуск

### 1. Установка зависимостей

```bash
npm install
```

### 2. Компиляция

```bash
npm run compile
```

### 3. Запуск в режиме разработки

1. Откройте проект в VS Code
2. Нажмите `F5` или выберите `Run > Start Debugging`
3. Откроется новое окно VS Code с установленным расширением

### 4. Настройка

В новом окне VS Code:

1. Откройте Command Palette (`Cmd+Shift+P` / `Ctrl+Shift+P`)
2. Выполните команду: **"CodeLab: Set API Token"**
3. Введите ваш API токен

### 5. Использование

#### Открыть чат
- Нажмите на иконку CodeLab в Activity Bar (боковая панель)
- Или выполните команду: **"CodeLab: Open Chat"**

#### Объяснить код
1. Выделите код в редакторе
2. Правый клик → **"CodeLab: Explain Selection"**

#### Рефакторинг
1. Выделите код в редакторе
2. Правый клик → **"CodeLab: Refactor Selection"**

#### Исправить ошибки
- Правый клик в редакторе → **"CodeLab: Fix Errors"**

#### Генерация кода
- Command Palette → **"CodeLab: Generate Code"**

## Настройки

Откройте VS Code Settings (`Cmd+,` / `Ctrl+,`) и найдите "CodeLab":

- **Base URL**: URL backend API (по умолчанию: `http://localhost:8000`)
- **Timeout**: Таймаут запросов в мс (по умолчанию: `30000`)
- **Retry Attempts**: Количество повторных попыток (по умолчанию: `3`)
- **Streaming Enabled**: Включить streaming (по умолчанию: `true`)
- **Max File Size**: Максимальный размер файла для контекста (по умолчанию: `1048576` байт)
- **Max Files**: Максимальное количество файлов (по умолчанию: `100`)
- **Max Context Size**: Максимальный размер контекста (по умолчанию: `10485760` байт)

## Разработка

### Запуск тестов

```bash
# Запустить все тесты
npm test

# Запустить тесты в watch режиме
npm run test:watch
```

### Проверка типов

```bash
npm run check-types
```

### Линтинг

```bash
npm run lint
```

### Сборка production версии

```bash
npm run package
```

Результат будет в папке `dist/`.

## Структура проекта

```
codelab-vscode-plugin/
├── src/
│   ├── api/              # API клиент и схемы
│   ├── commands/         # VS Code команды
│   ├── context/          # Сбор контекста проекта
│   ├── diff/             # Diff engine для применения изменений
│   ├── ui/               # UI компоненты (WebView)
│   ├── test/             # Тесты и моки
│   └── extension.ts      # Точка входа
├── resources/            # Ресурсы (иконки)
├── doc/                  # Документация
└── dist/                 # Скомпилированный код
```

## Отладка

### Логи

Откройте Output Panel (`Cmd+Shift+U` / `Ctrl+Shift+U`) и выберите "CodeLab" из выпадающего списка.

### Breakpoints

1. Установите breakpoint в коде (клик слева от номера строки)
2. Запустите в режиме отладки (`F5`)
3. Выполните действие, которое вызовет код с breakpoint

### DevTools для WebView

1. Откройте Command Palette
2. Выполните: **"Developer: Open Webview Developer Tools"**
3. Выберите CodeLab WebView

## Troubleshooting

### Ошибка "Cannot connect to backend"

- Убедитесь, что CodeLab Backend запущен на `http://localhost:8000`
- Проверьте настройку `codelab.api.baseUrl`
- Проверьте доступность API: откройте `http://localhost:8000/health` в браузере

### Ошибка "Invalid or expired token" / "Not authenticated"

Эта ошибка возникает, когда API токен не установлен или истёк. Для решения:

1. Откройте Command Palette (`Cmd+Shift+P` / `Ctrl+Shift+P`)
2. Выполните команду: **"CodeLab: Set API Token"**
3. Введите ваш API токен (он будет сохранён безопасно в VS Code Secrets)

**Где получить токен:**
- Обратитесь к администратору CodeLab Backend
- Или сгенерируйте токен через API Backend (если у вас есть доступ)

**Примечание:** При попытке создать новый чат или отправить сообщение без токена, расширение автоматически предложит установить токен.

### Расширение не активируется

- Проверьте консоль разработчика: `Help > Toggle Developer Tools`
- Посмотрите логи в Output Panel

## Дополнительная информация

- [README.md](./README.md) - Полное руководство
- [IMPLEMENTATION.md](./doc/IMPLEMENTATION.md) - Техническая документация
- [TESTING.md](./doc/TESTING.md) - Руководство по тестированию
- [CHANGELOG.md](./CHANGELOG.md) - История изменений

## Поддержка

Если у вас возникли проблемы:

1. Проверьте [TROUBLESHOOTING](./README.md#troubleshooting) в README
2. Посмотрите логи в Output Panel
3. Создайте issue в репозитории проекта
